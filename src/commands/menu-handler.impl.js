async function handleCommand(env, job) {
  // TODO: Implement menu command logic here.
  // Example: return await someAsyncOperation(job.data);
}

module.exports = { handleCommand };

Object.assign(module.exports,{handleCommand});

